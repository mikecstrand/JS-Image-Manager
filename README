Startings off a JS class for managing responsive images. jQuery required.

Usage:

var imageManage = new imageManager();
		imageManage.init();

Also :

Use the mobile detection with - imagemanage.isMobile() - Returns true/false